<%= form_for([@company,@client]) do |f| %>
  <% if @client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
      <% @client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        Proporcionar detalles del nuevo cliente
      </div>
      <div class="panel-body">
        <form role="form">
          <div class="row">
            <div class="col-lg-6 col-sm-6">
              <div class="form-group">
                <label>Nombre</label>
                <%= f.text_field :Name, class: "form-control" %>
              </div>
              <div class="form-group">
                <label>Dirección</label>
                <%= f.text_area :Address, class: "form-control" %>
              </div>
              <div class="form-group">
                <label>Tipo</label>
                <%= f.text_field :Type, class: "form-control" %>
              </div>
              <div class="form-group">
                <label>Dirección de facturación</label>
                <%= f.text_field :Billing_Address, class: "form-control" %>
              </div>
              <div class="form-group">
                <label>NIF</label>
                <%= f.text_field :NIF, class: "form-control" %>
              </div>
              <div class="form-group">
                <label>Verificado</label>
                <%= f.check_box :Verified, class: "checkbox" %>
              </div>
              <div class="form-group">
                <!-- <label>ID del Empresa</label> -->
                <%= f.text_field :company_id, class: "form-control", type: "hidden" %>
              </div>
              <div class="actions">
                <%= f.submit "Guardar Cliente", class: "btn btn-primary" %>
                <a href="<%= company_clients_path %>" class="page-scroll btn btn-primary">Atrás</a>
              </div>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

<% end %>
